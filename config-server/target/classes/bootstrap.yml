server:
  port: ${SERVER_PORT:8888}

spring:
  cloud:
    config:
      server:
        git:
          uri:
        native:
          search-locations: classpath:/microservice/dev

  profiles:
    active: dev

#server:
#  port: 8888

#spring:
#  application:
#    name: 'config-server'

#  profiles:
#    active: native

#  cloud:
#    config:
#      server:
#        bootstrap: true
#        native:
#          search-locations: classpath:/microservice

#  profiles:
#   active: dev